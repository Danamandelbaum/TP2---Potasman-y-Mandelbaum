
const PI = 3.14;
let arrayStrings = ["Dos" , "cuatro", "ocho" , "Diez" ]
function sumar(a, b) 
{
    const sumar = a + b;    
}
const multiplicar = (a, b) => 
{
    const multiplicar = a * b; 
};
const restar = (a, b) => 
{
    const restar = a - b; 
};
const dividir = (a, b) => 
{
    const dividir = a * b; 
};

export {PI, sumar, multiplicar, dividir, restar, arrayStrings};

